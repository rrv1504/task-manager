# Task Manager Frontend

React and Vite frontend for the task manager.

## Setup

1. Run `npm install`
2. Copy `.env.example` to `.env` if your API URL is different
3. Run `npm run dev`

The app runs at `http://localhost:5173`.
