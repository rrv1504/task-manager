# Task Manager Backend

Express API with MongoDB, JWT authentication, and user-owned tasks.

## Setup

1. Run `npm install`
2. Copy `.env.example` to `.env`
3. Update `MONGO_URI` and `JWT_SECRET`
4. Run `npm run dev`

The API runs at `http://localhost:5000`.
